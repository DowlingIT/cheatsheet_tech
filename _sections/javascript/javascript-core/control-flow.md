---
title: Control Flow
subtopic: javascript-core
group: Control Flow
order: 1
---

#### Conditionals

```js
if (x > 0) { } else if (x < 0) { } else { }

switch (status) {
  case 1:
  case 2:
    label = 'Active';
    break;
  default:
    label = 'Other';
}

const result = cond ? valueA : valueB;   // ternary
```

#### Jump

```
break  continue  return  throw
```
