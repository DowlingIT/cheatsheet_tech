---
title: Utility Functions
subtopic: jquery
group: Utilities & Plugins
order: 1
---

#### $.* helpers

```js
$.each(array, (i, val) => {});
$.extend({}, defaults, options);      // shallow merge, like Object.assign
$.isArray(val)   $.isFunction(val)
$.trim(str)
$.parseJSON(str)
```
