---
title: Buffer
subtopic: nodejs
group: Streams & Buffers
order: 2
---

#### Buffer

```js
Buffer.from('hello', 'utf8')
Buffer.from([72, 105])
buf.toString('utf8')
buf.length
buf.slice(0, 2)
Buffer.concat([buf1, buf2])
Buffer.isBuffer(val)
```
